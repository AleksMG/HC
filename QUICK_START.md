# ✅ Быстрый старт: Новые возможности HC Arena v3.0.0+

## 🚀 Запуск Arena прямо сейчас

**[👉 Открыть AI Arena в браузере (Live) →](https://htmlpreview.github.io/?https://github.com/AleksMG/HC/blob/codespace-solid-bassoon-7vw45496wrwq3p9qr/arena.html)**

**Или** запустить локально на вашем компьютере:
```bash
cd /workspaces/HC
python3 -m http.server 8000
# Затем откройте: http://localhost:8000/arena.html
```

---

## 🎯 Главные улучшения

### 1. **EnemyPredictor** — Предсказание ходов врага
Автоматический анализ поведения противника для выбора оптимальной тактики.

```javascript
// Предсказываемые категории поведения:
- 'movingRight' / 'movingLeft' / 'movingDown' / 'movingUp' — стандартное движение
- 'aggressive'  — враг атакует с высокой интенсивностью
- 'fleeing'     — враг убегает / у него мало здоровья
- 'unpredictable' — поведение не распознано
```

**Как агент это использует?**  
Каждый фрейм, когда враг на расстоянии < 150 пикселей, агент записывает его движение и анализирует паттерны последних 5 ходов.

---

### 2. **StrategyMemory** — Память о побеНных стратегиях
Сохраняет успешные комбинации (веса нейросети + тактика + соперник).

```javascript
// Пример: Agent Blue выиграл у Red с тактикой 'kite' и точностью 75%
agent.strategyMemory.saveWinningStrategy(
    agent.net.W1,    // Матрица весов, которая привела к победе
    'red',           // ID противника
    'kite',          // Какую тактику использовали
    0.75             // Как часто побеждали так
);

// Потом, когда встретится новый враг похожий на Red:
const strategy = agent.strategyMemory.loadSimilarStrategy('red');
if (strategy) {
    agent.net.W1 = strategy.weights; // Используем те же веса!
    agent.currentTactic = strategy.tactics;
}
```

---

### 3. **5 боевых тактик** с уникальным стилем движения

| Тактика | Механика | Пример |
|---------|----------|--------|
| **aggressive** (Лобовая атака) | `движение *= 1.3` — максимальная скорость в врага | Когда враг убегает или заканчивается здоровье |
| **kite** (Круговой маневр) | Движение перпендикулярно направлению на врага | Враг сильнее, нужна тактическая дистанция |
| **dodge** (Уклонение) | Движение под углом 60° от линии врага | Враг атакует, а ты слабый |
| **bait** (Провокация) | `движение *= 0.3` — медленное, "слабое" движение | Стандартная ситуация, ждем ошибки врага |
| **defensive** (Отступление) | `движение *= -0.6` — обратное движение | Здоровье критическое (<30%) |

**Визуализация выбора тактики:**
```
Враг далеко        → defaultTargeting (без тактики)
Враг на 100-150px  → decideCombatAction() выбирает тактику
Враг близко (<100) → executeTactic() применяет паттерн движения
Враг убегает       → aggressive (давим)
Враг атакует       → dodge/kite (увертываемся)
Мое здоровье < 30% → defensive (отступаем)
```

---

### 4. **Динамическое внимание (Attention Mask)**
Нейросеть автоматически переключает приоритеты восприятия:

```javascript
// Если враг далеко (> 50px):
attention = {
    combat: 0.1,     // 10% внимания на врага
    spatial: 0.5,    // 50% на карту препятствий
    memory: 0.2,     // 20% на историю опыта
    survival: 0.2    // 20% на выживание
}

// Если враг близко (< 50px):
// combat: 0.5+ (фокус на врага!)
// spatial: 0.3- (меньше отвлекаемся на карту)

// Если здоровье критическое:
// survival: 0.4+ (полный фокус на спасение)
```

---

## 📌 Как это интегрировано в игру?

### **Каждый фрейм (update):**
```
1. Агент решает, что делать (decide)
    ├─ Нейросеть выбирает режим (hunt/flee/combat...)
    └─ Если враг видно → анализируем его

2. Если враг < 150px → обновляем EnemyPredictor
    ├─ Записываем его движение (fx, fy, aggression)
    └─ Предсказываем тип поведения

3. Агент движится (move)
    ├─ Обычное движение + избегание препятствий
    └─ Если выбрана тактика → executeTactic() 
       (модифицирует вектор направления)

4. Нейросеть делает финальную коррекцию
    └─ Могу усилить/ослабить агрессию

5. Получаем награду
    └─ Хеббиевское обучение на успешных весах

6. Если кто-то умер → save strategy in StrategyMemory
```

---

## 🧪 Как протестировать?

### **Включить Debug режим:**
1. Нажать `🔍 Debug` кнопку
2. Кликнуть на одного из агентов (Blue или Red)
3. Смотреть информацию в левой панели:
   - **Mode** — текущий режим (hunt/flee/combat...)
   - **Tactic** — какую боевую тактику использует (если в режиме боя)
   - **Confidence** — уверенность предсказаний (0-1)

### **Смотреть логи боя:**
- Каждый раз в логе появляется сообщение типа:
  ```
  🏆 RED победил! Тактика: kite
  ```
- Это значит, что Red выжил используя круговой маневр

### **Экспортировать агента:**
- Кнопка `💾 Сохранить` теперь сохраняет:
  - Веса нейросети
  - Историю боев (против кого выигрывал)
  - Топ-5 успешных стратегий
  - Навыки и мышечную память

---

## 🔬 Технические детали

### EnemyPredictor состояние:
```javascript
{
    history: [50 ходов врага],
    patterns: {
        'movingRight': [вероятность],
        'movingLeft': [вероятность],
        ...
    }
}
```

### StrategyMemory состояние:
```javascript
{
    winningStrategies: [
        {
            weights: [...веса],
            against: 'red',
            tactics: 'aggressive',
            winRate: 0.75,
            timestamp: 1708...
        },
        ... (максимум 20)
    ],
    fightHistory: {
        'red': {wins: 12, losses: 3, bestAccuracy: 0.85},
        'blue': {wins: 8, losses: 5, ...}
    }
}
```

---

## 📊 Что это дает?

### **Для агента:**
✅ Адаптирует тактику в реальном времени  
✅ Учится на ошибках (EnemyPredictor)  
✅ Использует свой успешный опыт (StrategyMemory)  
✅ Развивает специализацию (одни на агрессию, другие на защиту)

### **Для игрока:**
✅ Более интеллектуальное поведение агентов  
✅ Видимые стратегические переходы  
✅ Долгосрочное обучение между сеансами  
✅ Интересные парадоксы (слабый агент может выиграть через хитрость!)

---

## 🚀 Дальнейшие улучшения (roadmap)

- **Multi-tacticmutation** — эволюция создает новые боевые стили
- **Tournament mode** — турниры с рейтингами
- **Attention visualization** — визуальное отображение фокуса нейросети
- **Genetic strategies** — скрещивание успешных стратегий двух агентов
- **Neural style specialization** — одни агенты становятся "берсеркерами", другие "тактиками"

---

**HC Arena v3.0.0+**  
Встроенный искусственный интеллект для реальных боев! 🧠⚔️
