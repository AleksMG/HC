# 🧠 HC Arena v3.0.0+ — Улучшения боевой системы и эволюции

## 📋 Что было добавлено?

### 1. **EnemyPredictor** класс (линия ~995)
Система предсказания ходов противника в реальном времени.

**Функций:**
- `recordMove()` — записывает движение врага
- `predictNextAction()` — предсказывает поведение (aggressive, fleeing, moving, etc.)
- `getWeakpoints()` — находит слабые моменты в защите врага

**Использование:**
```javascript
agent.enemyPredictor.recordMove(action, distance, health, aggression);
const prediction = agent.enemyPredictor.predictNextAction();
```

---

### 2. **StrategyMemory** класс (линия ~1055)
Долгосрочная память успешных стратегий боя.

**Функций:**
- `recordFight()` — записывает результаты боя
- `saveWinningStrategy()` — сохраняет побеНные системы (если точность > 65%)
- `loadSimilarStrategy()` — загружает похожую стратегию для нового врага

**Использование:**
```javascript
agent.strategyMemory.saveWinningStrategy(
    weights,           // Веса нейросети
    opponentId,        // ID врага
    'aggressive',      // Тактика
    0.75              // Точность
);
```

---

### 3. **Боевые тактики** (линия ~1363)

#### `selectCombatTactic()` 
Выбирает одну из 5 тактик боя:

| Тактика | Когда использовать | Эффект |
|---------|-------------------|--------|
| **aggressive** | Враг заканчивает | Максимальный импульс атаки |
| **kite** | Враг сильный | Круговой маневр вокруг врага |
| **dodge** | Враг атакует & мало здоровья | Перпендикулярное движение |
| **bait** | Обычный случай | Медленное движение, провокация ошибки |
| **defensive** | Здоровье < 30% | Отступление с готовностью к контратаке |

#### `executeTactic()` 
Применяет выбранную тактику к вектору движения (линия ~1570)

```javascript
// Внутри метода move():
if (this.currentTactic && this.currentTactic !== 'adaptive') {
    finalDir = this.executeTactic(finalDir, target, walls);
}
```

---

### 4. **Динамическое внимание** (линия ~1383)
Метод `computeAttentionMask()` фокусирует нейросетевое внимание в зависимости от ситуации:

```javascript
if (enemyDistance < 50) {
    mask.combat = 0.5 + closeness * 0.4;  // Фокусируемся на враге
    mask.spatial = 0.3 - closeness * 0.2; // Меньше внимания к карте
}
```

---

### 5. **Интеграция в игровой цикл** (линия ~1990 в методе `update()`)

Каждый frame:
1. **Обновляем EnemyPredictor** — если враг на расстоянии < 150
2. **Выбираем боевую тактику** — если враг < 100 близко
3. **Сохраняем стратегии-победители** — после завершения боя

```javascript
// В update():
if (distance < 150) {
    agent.enemyPredictor.recordMove(action, distance, health, aggression);
}

if (distance < 100) {
    agent.decideCombatAction(enemy, this);
}
```

---

### 6. **Сохранение и загрузка** (линия ~1886)
Методы `toJSON()` и `fromJSON()` теперь сохраняют:
- Victory strategies (топ-5)
- Fight history (статистика против каждого врага)

```javascript
// В toJSON():
strategyMemory: {
    winningStrategies: this.strategyMemory.winningStrategies.slice(0, 5),
    fightHistory: this.strategyMemory.fightHistory
}
```

---

## 🎯 Как это работает?

### Поток боя:

```
Agent A встретил Agent B
    ↓
EnemyPredictor анализирует движения B (speed, aggression)
    ↓
Выбираем тактику (aggressive/kite/dodge/bait/defensive)
    ↓
executeTactic() модифицирует вектор движения
    ↓
Нейросеть делает финальную коррекцию движения
    ↓
B побежден
    ↓
StrategyMemory сохраняет победную стратегию
    ↓
A эволюционирует с повышенным мутационным давлением
```

---

## 🔥 Новые возможности для игрока

### В режиме Debug (🔍 Debug кнопка):
- Смотрите текущую **тактику боя** агента
- Видите **предсказание** движений врага
- Анализируйте **историю побед**

### На панели управления:
- Все параметры обучения работают с новыми системами
- Сохранение/загрузка теперь включает боевые стратегии
- Экспорт агентов содержит всю память побед

---

## 📊 Внутренние улучшения

### EnemyPredictor
- **History max**: 50 ходов
- **Pattern types**: 7 (movingRight/Left/Down/Up, fleeing, aggressive, unpredictable)
- **Accuracy**: растет с каждым контактом

### StrategyMemory  
- **Max strategies**: 20
- **Win threshold**: > 65% точность
- **Reusability**: загружает похожие стратегии для нового врага

### Combat Tactics
- **5 типов** с разными модификаторами движения
- **Динамический выбор** на основе анализа врага и своего состояния
- **Smoothing**: движение применяется плавно через lerp

---

## 🧪 Тестирование

Чтобы увидеть новые системы в действии:

1. **Запустите арену**: ▶ Старт
2. **Включите Debug**: 🔍 Debug → клик на агента
3. **Смотрите режим "тактика"** → покажет текущую стратегию боя
4. **После боя** → смотрите логи "🏆 ... Тактика: [имя]"

Успешные агенты будут:
- Быстро адаптироваться к врагу
- Переключаться между тактиками
- Со временем развивать специализацию (одни на агрессию, другие на оборону)

---

## 🚀 Future Ideas (для дальнейшего развития)

- [ ] **Multi-agent memory** — агенты учатся от соседей
- [ ] **Tournament mode** — турниры с сохранением статистики
- [ ] **Tactic mutations** — случайные новые тактики через эволюцию
- [ ] **Neural attention visualization** — реальная визуализация фокуса внимания
- [ ] **Skill specialization** — агенты специализируются в боевых стилях

---

**Версия**: HC v3.0.0+  
**Дата**: Февраль 2026  
**Автор**: Evolution System
